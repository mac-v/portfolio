{
  "version": 2,
  "builds": [
    {
      "src": "api/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/staticfiles/$1"
    },
    {
      "src": "/(.*)",
      "dest": "api/wsgi.py"
    }
  ],
  "buildCommand": "echo 'Starting collectstatic...' && python manage.py collectstatic --noinput && echo 'Collectstatic completed!' && python manage.py makemigrations && python manage.py migrate"

}
